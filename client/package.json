{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build:wasm": "wasm-pack build && npm install",
    "build": "npm run build:wasm && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10",
    "postcss": "^8",
    "tailwindcss": "^3",
    "typescript": "^5",
    "vite": "^5",
    "vite-plugin-top-level-await": "^1",
    "vite-plugin-wasm": "^3"
  },
  "dependencies": {
    "@vercel/analytics": "^1",
    "quicktemp": "file:../pkg"
  }
}
